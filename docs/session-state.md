# Opika — where things stood

**Written:** 6 August 2026, as a pause brief after the M5 audit.
**Status:** superseded. `docs/build-plan.md` is the plan and the status ledger — its Part 1
history table folds in everything this brief reported that was still true, including the
"still merely asserted" items (seed-run verification, session security properties, M5's
non-geometry visual assertions) and the M2 index-usage contradiction, since resolved. Its
Part 3 timeline carries forward the actual gate on launch date: shelter recruitment, not
code. `docs/course-correction.md` carries the reasoning; this document does not need to.

This is kept, rather than deleted, for the same reason `course-correction.md` is: it named
a folder location for the design handoff that had already moved by the time it was read
again, and that specific error is the worked example behind `docs/standing-constraints.md`'s
"check a document's claims before relying on them" rule. The rest of its content —
PR status, test counts, the five gallery-contract gaps — has moved on since and is
tracked live in `docs/build-plan.md` instead of restated here.
